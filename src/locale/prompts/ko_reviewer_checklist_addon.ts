// ============================================
// OpenSwarm - Reviewer Checklist Addon (Korean)
// Source-string invariant test detection (single source of truth)
// ============================================

/**
 * 소스 문자열 기반 약한 가드 테스트 탐지용 체크리스트 항목.
 *
 * 동작 대신 소스 텍스트(`inspect.getsource`, `__code__`,
 * `readFile(__filename)` 등)를 검사하는 테스트는 약한 가드다 — 같은 로직을
 * 헬퍼 함수로 옮기면 통과한다. 이 항목은 리뷰어가 다음을 하도록 한다:
 *
 * 1. 이런 테스트를 불변식 증명이 아닌 약한 가드로 식별한다.
 * 2. 검사하는 토큰이 코드베이스에 실재하는 API 이름인지 확인한다.
 * 3. 테스트가 잡지 못하는 변형(예: 간접 호출)을 명시한다.
 */
export const sourceStringChecklistItemKo =
  `소스 문자열 불변식 테스트: 테스트가 소스 검사(\`inspect.getsource\`, \`__code__\`, \`readFile(__filename)\` 등)를 사용한다면, 검사하는 토큰이 코드베이스에 실제로 존재하는 API 이름인지 확인하고, 테스트가 잡지 *못하는* 변형(예: "헬퍼 함수를 통한 간접 호출")을 명시하라. 소스 텍스트만 검사하는 테스트는 약한 가드다 — 같은 로직을 헬퍼 함수로 옮기면 통과한다. 불변식 증명으로 취급하지 말고, 판정에 그 한계가 적힌 경우에만 승인하라. 값싼 동작 기반 대안(예: 금지 메서드가 호출되면 테스트가 실패하는 mock client)이 있다면 그쪽을 우선하라.`;